# Traefik dynamic configuration — global middlewares.
# Managed by `overwatch infra deploy`.

http:
  middlewares:
    hsts:
      headers:
        stsSeconds: 31536000
        stsIncludeSubdomains: true
        stsPreload: true
